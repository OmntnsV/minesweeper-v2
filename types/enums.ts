export enum Difficulty {
    Easy,
    Moderate,
    Hard,
    PureEvil
}

export enum FieldSize {
    Small,
    Medium,
    Large
}

export enum CellContent {
    Empty,
    One,
    Two,
    Three,
    Four,
    Five,
    Six,
    Seven,
    Eight,
    Mine,
    Null
}